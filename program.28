#include <iostream>
using namespace std;
int main() {
    float a,b,c;
    cout<< "Enter values of a, b and c: ";
    cin>>a>>b>>c;
    float disc=b*b - 4*a*c;
    cout<< "The discriminant is: " << disc << endl;
    return 0;
}